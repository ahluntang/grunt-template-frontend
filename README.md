# Personal grunt template to kickstart a front-end project


## Install

```
npm install
```

## Usage

Just run the default grunt task to minify, concat and move the assets to the `public/` folder.

```
grunt
```


